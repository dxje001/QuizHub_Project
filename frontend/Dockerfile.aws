# Simple production Dockerfile using pre-built dist folder
FROM nginx:alpine

# Copy pre-built frontend files
COPY dist /usr/share/nginx/html

# Copy nginx configuration
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
